# Shh

Native SwiftUI SSH client foundation for iPhone and iPad.

The implementation lives on `feat/ipados-ssh-client` in its dedicated worktree. The
project intentionally provides an offline/demo slice and keeps live SSH and Apple-only
services behind replaceable protocols.
