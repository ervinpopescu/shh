import SwiftUI

/// Shared semantic styles for compact content rows without opting out of Dynamic Type.
enum AppTypography {
    static let rowTitle = Font.body.weight(.semibold)
    static let rowSubtitle = Font.footnote
    static let rowMetadata = Font.caption
}
